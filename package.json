{
  "name": "agent-ctap-marketing",
  "private": true,
  "type": "module",
  "description": "One tree for Community Tap & Pizza / Never 86'd: owner portal, PDQ, photo invoices, mailbox routing, calendar, hunter bots.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "portal": "vite --config portal/vite.config.ts --host 0.0.0.0 --port 5173",
    "build": "vite build --config portal/vite.config.ts && cp portal/dist/index.html portal/dist/404.html && touch portal/dist/.nojekyll",
    "preview": "vite preview --config portal/vite.config.ts --host 0.0.0.0 --port 5173",
    "demo": "tsx scripts/demo-pdq-parse.ts",
    "nightly": "tsx scripts/nightly-loop.ts"
  },
  "devDependencies": {
    "@types/node": "^24.7.0",
    "tsx": "^4.19.2",
    "typescript": "5.9.3",
    "vite": "^6.3.5",
    "vitest": "^2.1.4"
  }
}
